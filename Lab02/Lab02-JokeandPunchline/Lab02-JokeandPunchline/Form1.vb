Public Class Form1
    Private Sub Button1_Click() Handles Button1.Click
        Label1.Text = "The optimist sees the glass as half full, the pessimist sees the glass as half empty, what does the engineer see the glass as?"
    End Sub
    Private Sub Button2_Click() Handles Button2.Click
        Label1.Text = "Twice as big as it needs to be."
    End Sub
End Class
